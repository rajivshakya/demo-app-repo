name: CI

on:
  push:
    branches:
      - feature

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run tests
        run: |
          echo "Running tests..."
          # yahan application ke actual tests add karna

      - name: Build Docker image
        run: |
          docker build -t demo-app:${{ github.sha }} .